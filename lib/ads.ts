/**
 * 애드센스 광고 단위.
 *
 * 퍼블리셔 ID 는 keywordegg 계열과 같은 계정이라 새로 승인받을 필요가 없다.
 * 슬롯은 이 사이트용으로 따로 만든 것을 쓴다 — 다른 사이트 슬롯을 그대로
 * 쓰면 리포트에서 수익이 섞여 어느 사이트가 버는지 구분되지 않는다.
 *
 * ─────────────────────────────────────────────────────────────────────────
 *  이 사이트는 요금표 사이에 광고를 끼우지 않는다.
 *
 *  유족이 상중에 보는 화면이다. 화장료 표와 빈소 임대료 표 사이에 광고가
 *  들어가면 무엇이 정보이고 무엇이 광고인지 흐려진다. 그래서 `top` 은
 *  **도입부 다음(숫자가 나오기 전)**, `middle` 은 **요금과 장소 정보를 모두
 *  보여준 다음**에만 둔다. 표 사이에는 넣지 않는다.
 * ─────────────────────────────────────────────────────────────────────────
 */
export const AD_CLIENT = "ca-pub-9130836798889522";

export const AD_SLOTS = {
  /** 장례(글지역) — 홈·허브 목록 화면 */
  home: "6667655123",
  /** 장례(상단) — 도입부 다음 */
  top: "6122372989",
  /** 장례(CTA) — 본문을 다 보여준 다음 */
  middle: "5354573459",
  /** 장례(h2) — 본문·목록 맨 끝 */
  bottom: "7205826690",
} as const;
